import { defineConfig } from 'vite';

export default defineConfig({
  // Relative base so the build works at any path, e.g. GitHub Pages
  // serving from https://<user>.github.io/Svartfrost/.
  base: './',
});
